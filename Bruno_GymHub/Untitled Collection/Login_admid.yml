info:
  name: Login_admid
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/api/login
  body:
    type: json
    data: |-
      {
        "email": "admin@gymhub.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
