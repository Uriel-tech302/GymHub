info:
  name: login del administrador
  type: http
  seq: 10

http:
  method: POST
  url: http://127.0.0.1:8000/api/login
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "admin@gymhub.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
